# 代码规范检查规则

## 已检测到的问题

### calculator.js 中的BUG列表：

1. **语法错误**
   - 第4行：缺少分号
   - 第9行：缺少分号  
   - 第13行：缺少分号
   - 第36行：缺少分号
   - 第60行：缺少分号

2. **严重错误**
   - 第17行：条件语句中使用了赋值操作符 `=` 而非比较操作符 `==`
   - 第49行：条件语句中使用了赋值操作符 `=` 而非比较操作符 `==`
   - 第23行：除法操作未检查除数是否为零

3. **类型强制转换问题**
   - 第17行：使用了宽松比较 `==`
   - 第34行：使用了宽松比较 `==`
   - 第54行：使用了宽松比较 `==`
   - 第55行：使用了宽松比较 `==`

4. **变量作用域问题**
   - 第59行：使用了 `var` 声明变量

5. **逻辑错误**
   - 第28行：数组越界（使用 `<=` 导致访问 `numbers[numbers.length]`）
   - 第38行：递归函数缺少负数保护

## 规则文件说明

- `code-style.yaml`：代码风格规范
- `security.json`：安全性和逻辑错误检查规则

## 测试方法

可以使用自动化工具扫描 calculator.js 文件，根据规则文件检测出所有预设的BUG。